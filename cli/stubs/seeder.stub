package {{ .Package }}

import (
	"context"

	"github.com/devituz/lagodev/database"
	"github.com/devituz/lagodev/seeder"
)

func init() { seeder.Register(&{{ .Name }}{}) }

type {{ .Name }} struct{}

func ({{ .Name }}) Name() string { return "{{ .Name }}" }

func ({{ .Name }}) Run(ctx context.Context, conn *database.Connection) error {
	// Write INSERT statements here, or call a factory:
	//   _, err := factories.UserFactory(conn).Count(10).Create(ctx)
	//   return err
	return nil
}
